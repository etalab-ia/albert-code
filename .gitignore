# Skills externes — synchronisées par runtime/agent-vm.runtime.sh depuis etalab-ia/skills
.claude/skills/
.opencode/skills/
.agents/skills/

# Config OpenCode locale projet (générée par install.sh, pas versionnée)
opencode.json
opencode.jsonc
# Sauvegardes locales de configuration (tests GLM, à garder sur disque)
opencode.json.bak*

# Runtime agent-vm local
.agent-vm.runtime.sh

# Clone de travail du moteur de VM (agent-vm est un dépôt séparé, plus vendorisé)
/agent-vm/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Secrets (ne jamais committer)
.env
.env.*
!.env.example
*.pem
*.key
